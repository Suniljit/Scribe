# Hugging Face access token with access to pyannote/speaker-diarization-3.1
# (accept model terms at huggingface.co, then create a token at
# huggingface.co/settings/tokens). Required for speaker diarization.
HF_TOKEN=hf_xxx

# Optional overrides — defaults shown
# TRANSCRIBE_WHISPER_MODEL=large-v3
# TRANSCRIBE_WHISPER_COMPUTE=int8
# TRANSCRIBE_WHISPER_DEVICE=cpu
# TRANSCRIBE_DATA_DIR=./data
